#include<bits/stdc++.h>
using namespace std;

typedef long long ll;


int main(){
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);

  ll n,m;

  cin>>n>>m;
  ll arr[n];
  for(ll i=0;i<n;i++){
    ll a;
    cin>>a;
    arr[i] = a;
  }

  

  return 0;
}
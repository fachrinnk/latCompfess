#include<iostream>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    long long a;
    cin>>a;
    if(a>=0&&a<=100)cout<<"YA\n";
    else cout<<"TIDAK\n";
    return 0;
}